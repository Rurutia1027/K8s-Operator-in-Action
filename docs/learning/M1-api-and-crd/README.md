# M1: API & CRD

Learn the Custom Resource Definition (CRD) API layer before running any controller.

| Issue | Folder | Runnable |
|-------|--------|----------|
| #1 | `issue-01-crd-api-types` | `go test` |
| #2 | `issue-02-install-crd` | `./run.sh` (needs kubectl) |

```bash
make -C learning test-issue-01
make -C learning test-issue-02   # needs cluster
```
